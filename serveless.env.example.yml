local:
  STAGE: local
  REGION: localhost
  DEBUG: true

test:
  STAGE: test
  REGION: ${env:TEST_AWS_REGION}
  DEBUG: true

dev:
  STAGE: dev
  REGION: ${env:DEV_AWS_REGION}
  DEBUG: true

prd:
  STAGE: prd
  REGION: ${env:PRD_AWS_REGION}
  DEBUG: false
